import { describe, it } from 'vitest'

describe('Nav', () =>
  it('should render', () => {
    // redner the component
    // find the elment use testing playground ext
    // assert the element logic
  }))
